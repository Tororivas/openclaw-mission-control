apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mission-control-postgres
  namespace: mission-control
spec:
  interval: 5m
  releaseName: mission-control-postgres
  chart:
    spec:
      chart: ./deploy/helm/mission-control-postgres
      sourceRef:
        kind: GitRepository
        name: openclaw-mission-control
        namespace: flux-system
      interval: 1m
  values: {}
