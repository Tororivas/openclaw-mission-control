apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: mission-control-frontend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: mission-control-frontend
  filterTags:
    pattern: "^[0-9]+$"
  policy:
    numerical:
      order: asc
